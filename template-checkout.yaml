- job-template:

    name: '{name}-checkout'
    scm:
        - git:
            url: '{gitrepo}'
            branches:
                - origin/master
    publishers:
        - trigger-parameterized-builds:
            - project: '{name}-sintax'
              current-parameters: False
              condition: stable
