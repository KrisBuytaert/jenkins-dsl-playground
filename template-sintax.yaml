- job-template:
    name: '{name}-sintax'
    scm:
        - workspace:
            parent-job: '{name}-checkout'
            criteria: Any





























- project:
    gitrepo: https://github.com/Inuits/jitsi-meet
    name: our-app
    jobs:
      - '{name}-checkout':
      - '{name}-sintax':
      - '{name}-compile':
      - '{name}-unittest':
      - '{name}-package':
      - '{name}-repoupload':
      - '{name}-hostupload':
      - '{name}-install':
      - '{name}-sanity':
     

